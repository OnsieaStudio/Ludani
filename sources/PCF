const int pcfCount = 0;
const float totalTexels = (pcfCount * 2.0 + 1.0) * (pcfCount * 2.0 + 1.0);
float size = 16.0f;
float texelSize = 1.0 / size;
float total = 0.0;
	
for(int x = -pcfCount; x <= pcfCount; x ++)
{
	for(int y = -pcfCount; y <= pcfCount; y ++)
	{
		float objectNearestLight = texture(
	}
}